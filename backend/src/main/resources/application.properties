spring.application.name=crm
server.port=${SERVER_PORT:9090}

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME}

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT}

#Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.out-of-order=false

#hilos virtuales
spring.threads.virtual.enabled=true

# Configuracion SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=anth87659@gmail.com
spring.mail.password=htnd coyg zkcv kmln
# Estas propiedades cambian para SSL:
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false
spring.mail.properties.mail.smtp.ssl.enable=true

# Email remitente personalizado
app.mail.from=anth87659@gmail.com

# Twilio - WhatsApp webhook signature validation
twilio.auth-token=${TWILIO_AUTH_TOKEN}

# Logging para debugging de email
logging.level.nocountry.crm.feature.email=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.level.jakarta.mail=DEBUG
logging.level.javax.mail=DEBUG

